import React from 'react'

const Button = ({title}) => {
  return (
    <div className='px-10 py-2 rounded-md bg-gray-500 text-white text-center font-semibold whitespace-nowrap'>{title}</div>
  )
}

export default Button